package com.thoughtworks.bootcamp;

public class HarmonicNumber {
    public double calculateHarmonicValue(int i) {
        if (i == 1) return 1;
        else
            return (1 + 1.0 / i);
    }
}
